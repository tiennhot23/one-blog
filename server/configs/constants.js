const limit_element = 10
const saltRounds = 10
const fileBaseURL = 'http://localhost:5000/file/'

module.exports = {
   limit_element: limit_element,
   saltRounds: saltRounds,
   fileBaseURL: fileBaseURL
}